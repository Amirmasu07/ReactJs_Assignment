import React, { Component } from 'react';

class ClassCompoNpm extends Component {

    render() {
        return (
            <>
                <p>NPM is short for node package manager, an online directory that contains the various already registered open-source packages. NPM modules consume the various functions as a third-party package when installed into an app using the NPM command npm install.</p>
                <p>npm is the world's largest Software Library.</p>
            </>
        );
    }
}

export default ClassCompoNpm;